import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Cilt Analizi · İpek Yolu Şifacısı',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
