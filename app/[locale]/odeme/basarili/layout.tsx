import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Ödeme Başarılı · İpek Yolu Şifacısı',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
